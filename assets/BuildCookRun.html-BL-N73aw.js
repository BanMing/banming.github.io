import{_ as t}from"./plugin-vue_export-helper-DlAUqK2U.js";import{e as d,f as o,o as r}from"./app-_JojGnWZ.js";const c={};function a(n,e){return r(),d("div",null,e[0]||(e[0]=[o(`<h1 id="buildcookrun-参数全详解" tabindex="-1"><a class="header-anchor" href="#buildcookrun-参数全详解"><span>BuildCookRun 参数全详解</span></a></h1><p>BuildCookRun 是 UAT(Unreal Automation Tool)里最常用的命令,一条命令把 Build / Cook / Stage / Package / Deploy / Run 这几步串起来。编辑器里的 &quot;Package Project&quot;、Project Launcher、UnrealFrontend 打包,底层走的都是它。</p><p>跑命令的入口:</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code><span class="line">Engine\\Build\\BatchFiles\\RunUAT.bat BuildCookRun -Help</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>-Help</code> 输出里参数没有排序,而且部分参数(比如 <code>-numclients</code>)早就废弃了什么都不做,加上 BuildCookRun 是从其它 UAT 命令继承来的,help 开头还会刷一堆 <code>Duplicated help parameter</code> 警告。所以下面我把参数按用途分组整理,常用的几个单独拎出来说。</p><p>帮助里对命令本身的描述:</p><blockquote><p>Builds/Cooks/Runs a project. 对非 uproject 工程,target 通过编译工程目录下的 target rule 文件来发现。如果没指定 <code>-map</code>,命令会去工程的 DefaultEngine.ini 找 DefaultMap,找不到再去 BaseEngine.ini,再找不到就回退到 <code>/Engine/Maps/Entry</code>。</p></blockquote><h2 id="最该先记住的几个参数" tabindex="-1"><a class="header-anchor" href="#最该先记住的几个参数"><span>最该先记住的几个参数</span></a></h2><p>平时手写打包命令,真正天天用到的就这几个:</p><table><thead><tr><th>参数</th><th>作用</th></tr></thead><tbody><tr><td><code>-project=Path</code></td><td>工程路径(必填),如 <code>-project=D:\\Projects\\MyProject.uproject</code></td></tr><tr><td><code>-platform=PlatformName</code></td><td>目标平台,Windows 一般填 <code>Win64</code>(等价 <code>-targetplatform</code>)</td></tr><tr><td><code>-clientconfig=</code> / <code>-configuration=</code></td><td>构建配置:Development / Test / Shipping(源码版还有 Debug)</td></tr><tr><td><code>-build</code></td><td>执行编译步骤(C++ 工程需要,蓝图工程可省)</td></tr><tr><td><code>-cook</code></td><td>Cook 内容。去掉 uasset 里只有编辑器才需要的数据,顺带编译过期 shader</td></tr><tr><td><code>-stage</code></td><td>把 Cook 后的内容拷到 staging 目录,加上可执行文件、可选的 Prereq 安装器</td></tr><tr><td><code>-pak</code></td><td>把 Cook 后的内容塞进 <code>.pak</code>,可压缩、可加密</td></tr><tr><td><code>-archive</code> + <code>-archivedirectory=Path</code></td><td>把 staging 结果再拷一份到指定归档目录</td></tr><tr><td><code>-clean</code></td><td>编译前清掉中间文件 + 上一次 Cook/Stage 结果</td></tr></tbody></table><p>调试时的一个小技巧:<strong>临时去掉 <code>-pak</code></strong>,Stage 出来的资源会以&quot;散文件&quot;形式按 Content 目录结构铺开,方便你确认哪些资源被 Cook/Stage 了、哪些漏了。</p><p>一条典型的 Win64 打包命令长这样:</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code><span class="line">RunUAT.bat BuildCookRun -project=&quot;D:\\Projects\\MyProject.uproject&quot; -platform=Win64 -clientconfig=Development -cook -build -stage -pak -archive -archivedirectory=&quot;D:\\Archive&quot;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>关于 Cook 什么:编辑器菜单的 &quot;Package Project&quot; 会把工程里<strong>所有内容</strong>都 Cook 进去,不管用没用到,包会变得很大。用 <code>-map=</code> 指定要 Cook 的关卡,Cooker 会从关卡引用的资源开始一路&quot;spider out&quot;,只 Cook 真正被引用到的东西。代价是运行时动态加载、没有硬引用的资源(比如换肤用的 skin)不会被 Cook——这种要在 <code>DefaultGame.ini</code> 的 <code>[/Script/UnrealEd.ProjectPackagingSettings]</code> 里用 <code>+DirectoriesToAlwaysCook=</code> 把目录加进强制 Cook 列表。</p><h2 id="工程与平台" tabindex="-1"><a class="header-anchor" href="#工程与平台"><span>工程与平台</span></a></h2><table><thead><tr><th>参数</th><th>说明</th></tr></thead><tbody><tr><td><code>-project=Path</code></td><td>工程路径(必填),<code>-project=QAGame</code> / <code>-project=Samples\\BlackJack\\BlackJack.uproject</code> / 绝对路径均可</td></tr><tr><td><code>-destsample</code></td><td>目标 Sample 名</td></tr><tr><td><code>-foreigndest</code></td><td>Foreign 目标路径</td></tr><tr><td><code>-targetplatform=PlatformName</code></td><td>Build/Cook/Deploy 的目标平台(也写作 <code>-Platform</code>)</td></tr><tr><td><code>-servertargetplatform=PlatformName</code></td><td>专用服务器的目标平台(也写作 <code>-ServerPlatform</code>)</td></tr><tr><td><code>-foreign</code></td><td>从 blankproject 生成一个 foreign uproject 并使用它</td></tr><tr><td><code>-foreigncode</code></td><td>从 platformergame 生成一个 foreign code uproject 并使用它</td></tr><tr><td><code>-CrashReporter</code></td><td>是否一并编译 CrashReporter</td></tr><tr><td><code>-SpecifiedArchitecture</code></td><td>指定一个最低 OS / 架构</td></tr><tr><td><code>-Licensee</code></td><td>标记此构建由 licensee 编译</td></tr></tbody></table><h2 id="编译-build" tabindex="-1"><a class="header-anchor" href="#编译-build"><span>编译(Build)</span></a></h2><table><thead><tr><th>参数</th><th>说明</th></tr></thead><tbody><tr><td><code>-build</code></td><td>执行编译步骤</td></tr><tr><td><code>-noxge</code></td><td>不使用 XGE 做分布式编译</td></tr><tr><td><code>-ForceMonolithic</code></td><td>把结果合并成单个可执行文件</td></tr><tr><td><code>-ForceDebugInfo</code></td><td>即使 Development 也强制生成 debug info</td></tr><tr><td><code>-ForceNonUnity</code></td><td>关闭 unity build</td></tr><tr><td><code>-ForceUnity</code></td><td>强制开启 unity build</td></tr><tr><td><code>-nodebuginfo</code></td><td>不把 debug 文件拷到 stage</td></tr><tr><td><code>-separatedebuginfo</code></td><td>debug info 输出到单独目录</td></tr><tr><td><code>-MapFile</code></td><td>生成 <code>*.map</code> 文件</td></tr><tr><td><code>-UbtArgs</code></td><td>透传给 UBT 的额外参数</td></tr><tr><td><code>-NoSign</code></td><td>跳过 code/content 文件签名</td></tr></tbody></table><h2 id="cook" tabindex="-1"><a class="header-anchor" href="#cook"><span>Cook</span></a></h2><table><thead><tr><th>参数</th><th>说明</th></tr></thead><tbody><tr><td><code>-cook</code> / <code>-cookonthefly</code></td><td>决定构建是否使用 cooked 数据;<code>-cookonthefly</code> 让客户端用 cook-on-the-fly 服务器提供的数据运行</td></tr><tr><td><code>-skipcook</code></td><td>用 cooked 构建,但假设 cooked 数据已最新且就位(隐含 <code>-cook</code>)</td></tr><tr><td><code>-skipcookonthefly</code></td><td>cookonthefly 构建里,仅用于把信息传给 package 步骤</td></tr><tr><td><code>-Cookontheflystreaming</code></td><td>流式 cook-on-the-fly,不本地缓存,每次加载都从服务器重取</td></tr><tr><td><code>-iterativecooking</code> / <code>-iterate</code></td><td>增量 Cook</td></tr><tr><td><code>-CookPartialgc</code></td><td>Cook 时随用随清包,而不是空间不够时才一次性清</td></tr><tr><td><code>-CookInEditor</code></td><td>是否在编辑器里 Cook 而非 UAT 里</td></tr><tr><td><code>-IgnoreCookErrors</code></td><td>忽略 Cook 错误继续打包</td></tr><tr><td><code>-CookMapsOnly</code></td><td>只 Cook 地图(仅影响 <code>-cookall</code> 的行为)</td></tr><tr><td><code>-CookAll</code></td><td>Cook 工程 Content 目录下所有内容</td></tr><tr><td><code>-SkipCookingEditorContent</code></td><td>跳过 <code>/Engine/Editor</code> 下的内容</td></tr><tr><td><code>-FastCook</code></td><td>目标平台支持的话走 fast cook 路径</td></tr><tr><td><code>-IgnoreVerCheck</code></td><td>忽略版本检查</td></tr><tr><td><code>-IgnoreLightMapErrors</code></td><td>是否把 Light Map 错误当致命错误</td></tr><tr><td><code>-MapsToRebuildLightMaps</code></td><td>需要重建光照贴图的地图列表</td></tr><tr><td><code>-MapsToRebuildHLODMaps</code></td><td>需要重建 HLOD 的地图列表</td></tr></tbody></table><h2 id="pak-与-iostore" tabindex="-1"><a class="header-anchor" href="#pak-与-iostore"><span>Pak 与 IoStore</span></a></h2><table><thead><tr><th>参数</th><th>说明</th></tr></thead><tbody><tr><td><code>-pak</code></td><td>生成 pak 文件</td></tr><tr><td><code>-iostore</code></td><td>生成 I/O store 容器文件</td></tr><tr><td><code>-skippak</code></td><td>用 pak,但假设已构建好(隐含 <code>-pak</code>)</td></tr><tr><td><code>-skipiostore</code></td><td>覆盖 <code>-iostore</code>,不执行</td></tr><tr><td><code>-prepak</code></td><td>尽量不 Cook,改从网络拉 pak(隐含 <code>-pak</code> 和 <code>-skipcook</code>)</td></tr><tr><td><code>-signpak=keys</code></td><td>用指定 key 给 pak 签名,如 <code>-signpak=C:\\Encryption.keys</code>(隐含 <code>-signedpak</code>)</td></tr><tr><td><code>-signed</code></td><td>游戏预期使用签名 pak</td></tr><tr><td><code>-PakAlignForMemoryMapping</code></td><td>为 bulk data 内存映射做对齐</td></tr></tbody></table><h2 id="stage-package-archive-deploy" tabindex="-1"><a class="header-anchor" href="#stage-package-archive-deploy"><span>Stage / Package / Archive / Deploy</span></a></h2><table><thead><tr><th>参数</th><th>说明</th></tr></thead><tbody><tr><td><code>-stage</code></td><td>放入 stage 目录</td></tr><tr><td><code>-skipstage</code></td><td>用 stage 目录,但假设东西都在(隐含 <code>-stage</code>)</td></tr><tr><td><code>-nocleanstage</code></td><td>不清理 stage 目录</td></tr><tr><td><code>-stagingdirectory=Path</code></td><td>构建拷贝目录,如 <code>-stagingdirectory=C:\\Stage</code></td></tr><tr><td><code>-package</code></td><td>为目标平台打包</td></tr><tr><td><code>-skippackage</code></td><td>跳过打包</td></tr><tr><td><code>-distribution</code></td><td>打发行版本</td></tr><tr><td><code>-prereqs</code></td><td>把前置依赖一起 stage</td></tr><tr><td><code>-applocaldir</code></td><td>applocal 部署的前置依赖位置</td></tr><tr><td><code>-Prebuilt</code></td><td>这是一个预构建的 cooked &amp; packaged 版本</td></tr><tr><td><code>-AdditionalPackageOptions</code></td><td>传给平台 packager 的额外选项</td></tr><tr><td><code>-archive</code></td><td>放入 archive 目录</td></tr><tr><td><code>-archivedirectory=Path</code></td><td>归档目录,如 <code>-archivedirectory=C:\\Archive</code></td></tr><tr><td><code>-archivemetadata</code></td><td>归档时额外带上元数据文件(如 build.properties)</td></tr><tr><td><code>-createappbundle</code></td><td>Mac 归档时打成 <code>.app</code> 而不是散文件</td></tr><tr><td><code>-manifests</code></td><td>Cook 时生成 streaming install manifest</td></tr><tr><td><code>-createchunkinstall</code></td><td>从 manifest 生成 streaming install 数据(需 <code>-stage</code> &amp; <code>-manifests</code>)</td></tr><tr><td><code>-deploy</code></td><td>部署到目标平台</td></tr><tr><td><code>-getfile</code></td><td>运行成功后从目标设备下载文件</td></tr><tr><td><code>-bundlename</code></td><td>部署到移动设备时用的 bundle 名</td></tr></tbody></table><h2 id="运行-run-与测试" tabindex="-1"><a class="header-anchor" href="#运行-run-与测试"><span>运行(Run)与测试</span></a></h2><table><thead><tr><th>参数</th><th>说明</th></tr></thead><tbody><tr><td><code>-run</code></td><td>构建完后运行游戏(有 <code>-server</code> 时连服务器一起)</td></tr><tr><td><code>-fileserver</code></td><td>客户端用 UnrealFileServer 提供的 cooked 数据运行</td></tr><tr><td><code>-dedicatedserver</code> / <code>-server</code></td><td>Build/Cook/Run 客户端 + 服务器</td></tr><tr><td><code>-client</code></td><td>Build/Cook/Run 客户端 + 服务器,用 client target 配置</td></tr><tr><td><code>-noclient</code></td><td>不跑客户端,只跑服务器</td></tr><tr><td><code>-skipserver</code></td><td>跳过启动服务器</td></tr><tr><td><code>-logwindow</code></td><td>给客户端开日志窗口</td></tr><tr><td><code>-map</code></td><td>运行用的地图</td></tr><tr><td><code>-AdditionalServerMapParams</code></td><td>额外的 server map 参数,如 <code>?param=value</code></td></tr><tr><td><code>-device</code> / <code>-serverdevice</code></td><td>运行游戏 / 服务器的设备</td></tr><tr><td><code>-numclients=n</code></td><td>多开客户端,n &gt;= 2(注:已废弃)</td></tr><tr><td><code>-addcmdline</code> / <code>-servercmdline</code> / <code>-clientcmdline</code></td><td>追加 / 覆盖命令行参数</td></tr><tr><td><code>-nullrhi</code></td><td>给客户端命令行加 <code>-nullrhi</code></td></tr><tr><td><code>-fakeclient</code></td><td>给 server URL 加 <code>?fake</code></td></tr><tr><td><code>-editortest</code></td><td>不跑客户端,改跑编辑器</td></tr><tr><td><code>-RunAutomationTests</code></td><td>跑 <code>-editortest</code> 或客户端时跑全部 automation test(与 <code>-server</code> 不兼容)</td></tr><tr><td><code>-Crash=index</code></td><td>按 index 注入 debug crash / rendercrash 等命令</td></tr><tr><td><code>-deviceuser</code> / <code>-devicepass</code></td><td>Linux 无人值守的用户名 / 密码</td></tr><tr><td><code>-RunTimeoutSeconds</code></td><td>启动游戏后的等待超时</td></tr></tbody></table><h2 id="其它" tabindex="-1"><a class="header-anchor" href="#其它"><span>其它</span></a></h2><table><thead><tr><th>参数</th><th>说明</th></tr></thead><tbody><tr><td><code>-unattended</code></td><td>假设无人值守,绝不卡住等待</td></tr><tr><td><code>-cmdline</code></td><td>写进 stage 内 <code>UE4CommandLine.txt</code> 的命令行</td></tr><tr><td><code>-ue4exe=ExecutableName</code></td><td>UE 编辑器可执行文件名,如 <code>-ue4exe=UE4Editor.exe</code></td></tr></tbody></table><h2 id="打包时各阶段产物落在哪" tabindex="-1"><a class="header-anchor" href="#打包时各阶段产物落在哪"><span>打包时各阶段产物落在哪</span></a></h2><p>把 BuildCookRun 跑通后,大致流程和落盘位置:</p><ul><li><strong>编译源码</strong>:Development / Test / Shipping 可执行文件落在工程 <code>Binaries/&lt;平台&gt;</code> 下(Win64 是 <code>Binaries/Win64</code>),之后会被拷进 staged 目录。</li><li><strong>Cook 内容</strong>:落在工程 <code>Saved/Cooked/&lt;平台&gt;</code>。非源码版只能选 &quot;no editor&quot; 类型,Windows 对应 <code>Saved/Cooked/WindowsNoEditor</code>;源码版还能 Cook dedicated server(<code>WindowsServer</code>)/ dedicated client(<code>WindowsClient</code>)。</li><li><strong>Stage</strong>:把 cooked 文件拷进 <code>Saved/StagedBuilds/WindowsNoEditor</code>,可选打进 <code>.pak</code>。staged 根目录会有一堆 Manifest <code>.txt</code>(运行不需要,可删),还有 bootstrap 可执行文件——它只是个方便的启动器,会自动装 Prereq(DirectX、C 运行库)。</li><li><strong>Archive</strong>:<code>-archive</code> + <code>-archivedirectory=</code> 把 staged 目录整份再拷一份到你指定的位置,方便保留多个版本。</li></ul><p>ExitCode 这个细节值得记一下:UAT 总会带一个 ExitCode 退出,0 是 Success。BuildCookRun 跑 BuildGame 时底层调 UBT,编译失败 UBT 返回非 0,UAT 也跟着返回非 0——批处理 / CI 就靠这个判断构建成败。</p><hr><h1 id="加速-cook-参数-speed-up-cook" tabindex="-1"><a class="header-anchor" href="#加速-cook-参数-speed-up-cook"><span>加速 Cook 参数(Speed Up Cook)</span></a></h1><p>下面三个 <code>-Sqex*</code> 参数是定制引擎里加的加速 Cook 开关,核心思路都是&quot;用稳定性换速度&quot;,适合开发期快速迭代,临近发版要谨慎。</p><h2 id="sqexnevergc" tabindex="-1"><a class="header-anchor" href="#sqexnevergc"><span>-SqexNeverGC</span></a></h2><p>在 Cook 阶段禁用垃圾回收(GC)。正常情况下 GC 自动回收不再用的内存防止泄漏,但 Cook 大量数据 / 复杂资源时,频繁 GC 会拖慢速度。关掉 GC 能加快 Cook,代价是<strong>内存占用显著上涨</strong>——内存紧张的机器上反而可能更糟,用之前先看机器内存够不够。</p><h2 id="sqexquickexit" tabindex="-1"><a class="header-anchor" href="#sqexquickexit"><span>-SqexQuickExit</span></a></h2><p>让引擎 Cook 完之后快速退出,跳过正常的清理和关闭流程(释放内存、保存状态等)。自动化 / 批处理任务里这些收尾常常没必要,省下来就是时间。风险是出错或异常终止时某些资源可能没被正确清理,常规开发测试里<strong>慎用</strong>。</p><h2 id="sqexskipverifyedl" tabindex="-1"><a class="header-anchor" href="#sqexskipverifyedl"><span>-SqexSkipVerifyEDL</span></a></h2><p>EDL(Editor Dependency List,编辑器依赖列表)是 UE 用来追踪资源间依赖关系的机制。Cook 时引擎默认会校验 EDL,确保该打进包的资源都正确处理了,防止运行时缺资源 / 报错——但这个校验本身耗时。<code>-SqexSkipVerifyEDL</code> 就是跳过这步校验,换来更快的 Cook,代价是可能漏掉关键依赖,导致运行时报错或崩溃。开发初期、快速迭代可以用;<strong>准备最终发版时建议不要用</strong>,确保依赖关系都处理到位。</p><hr><h1 id="cook-命令拆解-shader-与-blueprint" tabindex="-1"><a class="header-anchor" href="#cook-命令拆解-shader-与-blueprint"><span>Cook 命令拆解:Shader 与 Blueprint</span></a></h1><p>打包里 Cook 这一步内部其实是引擎跑特定 commandlet。把它拆成单独命令手动跑,既能定位问题,也方便预热 shader / 蓝图。下面用 <code>&lt;workspace&gt;</code> 代表引擎根目录占位符。</p><h2 id="命令" tabindex="-1"><a class="header-anchor" href="#命令"><span>命令</span></a></h2><p>Cook Shader:</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code><span class="line">&lt;workspace&gt;\\Engine\\Binaries\\Win64\\UE4Editor-Cmd.exe &lt;workspace&gt;\\Game\\Game.uproject -run=CookGlobalShaders -platform=Windows -utf8output</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Cook Blueprint:</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code><span class="line">&lt;workspace&gt;\\Engine\\Binaries\\Win64\\UE4Editor-Cmd.exe &lt;workspace&gt;\\Game\\Game.uproject -run=CompileAllBlueprints -utf8output</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h2 id="日志分析" tabindex="-1"><a class="header-anchor" href="#日志分析"><span>日志分析</span></a></h2><p><strong>Cook Shader</strong>:这条命令会编译 <code>LogTargetPlatformManager</code>、<code>LogClass</code>、<code>LogAutomationTest</code>、<code>LogInit</code>、<code>LogShaderCompilers</code>。在 MyProject 工程里平均耗时约 1~5 分钟。</p><p><strong>Cook Blueprint</strong>:内容会包含 Shader 那部分,外加 <code>LogCompileAllBlueprintsCommandlet</code>、<code>LogBlueprint</code>、<code>LogAudioDerivedData</code>、<code>LogSkeletalMesh</code>,而且会编译那些 Cook 里实际不用的资源。MyProject 工程里平均耗时约 30~50 分钟,明显比 Shader 慢很多。</p><h2 id="compileallblueprints-是怎么找出来的" tabindex="-1"><a class="header-anchor" href="#compileallblueprints-是怎么找出来的"><span>CompileAllBlueprints 是怎么找出来的</span></a></h2><p><code>-run=CompileAllBlueprints</code> 这个名字不是凭空来的,可以从引擎源码里反推:</p><ol><li>全局搜 <code>commandlet</code>,定位到 <code>Engine\\Source\\Editor\\UnrealEd\\Private\\Commandlets\\CookCommandlet.cpp</code>。</li><li>顺着里面的定义,找到 <code>Engine\\Source\\Editor\\UnrealEd\\Private\\Commandlets\\CompileAllBlueprintsCommandlet.cpp</code>。</li><li>该 cpp 里的 commandlet 定义,对应的 <code>-run</code> 名就是 <code>CompileAllBlueprints</code>。</li></ol><p>记住这个套路:任何 <code>-run=XXX</code> commandlet,都能在 <code>UnrealEd/Private/Commandlets/</code> 下找到对应的 <code>*Commandlet.cpp</code> 确认它干了什么。</p>`,56)]))}const l=t(c,[["render",a],["__file","BuildCookRun.html.vue"]]),p=JSON.parse('{"path":"/GameEngine/Unreal/build/BuildCookRun.html","title":"BuildCookRun 参数全详解","lang":"en-US","frontmatter":{"date":"2024-11-17T20:32:56.000Z","tag":["Unreal Engine","Build"]},"git":{"createdTime":1782480255000,"updatedTime":1782480255000,"contributors":[{"name":"BanMing","username":"BanMing","email":"ban-ming@foxmail.com","commits":1,"url":"https://github.com/BanMing"},{"name":"Claude Opus 4.7","username":"Claude Opus 4.7","email":"noreply@anthropic.com","commits":1,"url":"https://github.com/Claude Opus 4.7"}]},"readingTime":{"minutes":8.74,"words":2621},"filePathRelative":"GameEngine/Unreal/build/BuildCookRun.md","localizedDate":"November 17, 2024","excerpt":"\\n<p>BuildCookRun 是 UAT(Unreal Automation Tool)里最常用的命令,一条命令把 Build / Cook / Stage / Package / Deploy / Run 这几步串起来。编辑器里的 \\"Package Project\\"、Project Launcher、UnrealFrontend 打包,底层走的都是它。</p>\\n<p>跑命令的入口:</p>\\n<div class=\\"language-text line-numbers-mode\\" data-highlighter=\\"prismjs\\" data-ext=\\"text\\"><pre><code><span class=\\"line\\">Engine\\\\Build\\\\BatchFiles\\\\RunUAT.bat BuildCookRun -Help</span>\\n<span class=\\"line\\"></span></code></pre>\\n<div class=\\"line-numbers\\" aria-hidden=\\"true\\" style=\\"counter-reset:line-number 0\\"><div class=\\"line-number\\"></div></div></div>"}');export{l as comp,p as data};

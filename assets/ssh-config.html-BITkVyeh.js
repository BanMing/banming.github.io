import{_ as n}from"./plugin-vue_export-helper-DlAUqK2U.js";import{e,f as a,o as i}from"./app-BeQLnLll.js";const t={};function o(c,s){return i(),e("div",null,s[0]||(s[0]=[a(`<h1 id="linux-ssh-登录配置" tabindex="-1"><a class="header-anchor" href="#linux-ssh-登录配置"><span>Linux SSH 登录配置</span></a></h1><p>记录两件常做的事:让一台 Linux 能用密码或密钥 SSH 进另一台、在没有 root 的 Ubuntu 上启用 root 登录和免密。两者都绕不开 <code>/etc/ssh/sshd_config</code>。</p><h2 id="检查远端-ssh-配置" tabindex="-1"><a class="header-anchor" href="#检查远端-ssh-配置"><span>检查远端 SSH 配置</span></a></h2><p>连不上时,先别急着折腾本地。很多服务器默认就不允许密码认证,或者被安全软件/策略禁掉了密钥连接。先看远端 <code>sshd_config</code>:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line"><span class="token function">sudo</span> <span class="token function">grep</span> <span class="token string">&#39;PermitRootLogin&#39;</span> /etc/ssh/sshd_config</span>
<span class="line"><span class="token function">sudo</span> <span class="token function">grep</span> <span class="token string">&#39;PasswordAuthentication&#39;</span> /etc/ssh/sshd_config</span>
<span class="line"><span class="token function">sudo</span> <span class="token function">vim</span> /etc/ssh/sshd_config</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>两个关键项:</p><ul><li><code>PermitRootLogin</code> 设为 <code>yes</code> 允许 root 直接登录,<code>without-password</code> 表示 root 只能用密钥(更安全)。</li><li><code>PasswordAuthentication</code> 设为 <code>yes</code> 才允许密码登录;只想用密钥就设 <code>no</code>。</li></ul><p>改完重启服务,再核对密钥指纹:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line"><span class="token function">sudo</span> systemctl restart sshd</span>
<span class="line"><span class="token comment"># 验证密钥指纹</span></span>
<span class="line">ssh-keygen <span class="token parameter variable">-lf</span> ~/.ssh/authorized_keys</span>
<span class="line">ssh-keygen <span class="token parameter variable">-lf</span> ~/.ssh/id_rsa</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="ubuntu-从零启用-root-与免密登录" tabindex="-1"><a class="header-anchor" href="#ubuntu-从零启用-root-与免密登录"><span>Ubuntu:从零启用 root 与免密登录</span></a></h2><p>全新的 Ubuntu 默认没启用 root,GUI 也不让 root 登录。下面把这条路打通。</p><p>设置 root 密码:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line"><span class="token function">sudo</span> <span class="token function">passwd</span>     <span class="token comment"># 先输当前用户密码,再设 root 密码</span></span>
<span class="line"><span class="token function">su</span> root         <span class="token comment"># 用刚设的密码切到 root</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>全新系统可能连 vim 都没有,先更新源再装:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line"><span class="token function">sudo</span> <span class="token function">apt-get</span> update</span>
<span class="line"><span class="token function">sudo</span> <span class="token function">apt-get</span> <span class="token function">install</span> <span class="token function">vim</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>启用 root 的 GUI 登录,改 lightdm 配置 <code>/usr/share/lightdm/lightdm.conf.d/50-ubuntu.conf</code>:</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code><span class="line">greeter-show-manual-login=true   # 手工输入用户名和密码</span>
<span class="line">all-guest=false                  # 不允许 guest 登录</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>再注释掉 <code>/etc/pam.d/gdm-autologin</code> 和 <code>/etc/pam.d/gdm-password</code> 里阻止 root 登录的那一行(通常是第二行,以 <code>auth required pam_succeed_if.so user != root</code> 之类开头)。</p><blockquote><p>vim 里整行注释技巧:<code>Ctrl+V</code> 进 visual block,方向键选中要注释的行,<code>Shift+I</code> 进插入模式,在行首加 <code>#</code>,<code>Esc</code> 生效。</p></blockquote><p>最后改 <code>/root/.profile</code>,把 <code>mesg n || true</code> 改成 <code>tty -s &amp;&amp; mesg n || true</code>,否则 root 登录时会报错。</p><h3 id="配置-ssh-免密" tabindex="-1"><a class="header-anchor" href="#配置-ssh-免密"><span>配置 SSH 免密</span></a></h3><p>确认 ssh 服务装好了:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line"><span class="token function">ps</span> <span class="token parameter variable">-A</span> <span class="token operator">|</span> <span class="token function">grep</span> <span class="token function">ssh</span>    <span class="token comment"># 看到 sshd 说明服务在</span></span>
<span class="line"><span class="token comment"># 没有的话:</span></span>
<span class="line"><span class="token function">sudo</span> <span class="token function">apt-get</span> update <span class="token operator">&amp;&amp;</span> <span class="token function">sudo</span> <span class="token function">apt-get</span> <span class="token function">install</span> openssh-server</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>生成密钥并把公钥放进授权文件:</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line">ssh-keygen          <span class="token comment"># 一路回车</span></span>
<span class="line"><span class="token builtin class-name">cd</span> ~/.ssh/</span>
<span class="line"><span class="token function">cp</span> id_rsa.pub authorized_keys</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在 <code>/etc/ssh/sshd_config</code> 里确认这几行没被注释掉(<code>#RSAAuthentication</code> 这项较老的版本可能没有,忽略即可):</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code><span class="line">PubkeyAuthentication yes</span>
<span class="line">AuthorizedKeysFile .ssh/authorized_keys</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>重启 SSH 服务生效。私钥导到本地客户端就能免密登录了。</p>`,28)]))}const d=n(t,[["render",o],["__file","ssh-config.html.vue"]]),r=JSON.parse(`{"path":"/Basic/os/linux/ssh-config.html","title":"Linux SSH 登录配置","lang":"en-US","frontmatter":{"date":"2024-03-07T18:33:15.000Z","tag":["Linux"]},"git":{"createdTime":1782546817000,"updatedTime":1782546817000,"contributors":[{"name":"BanMing","username":"BanMing","email":"ban-ming@foxmail.com","commits":1,"url":"https://github.com/BanMing"},{"name":"Claude Opus 4.7","username":"Claude Opus 4.7","email":"noreply@anthropic.com","commits":1,"url":"https://github.com/Claude Opus 4.7"}]},"readingTime":{"minutes":1.86,"words":559},"filePathRelative":"Basic/os/linux/ssh-config.md","localizedDate":"March 7, 2024","excerpt":"\\n<p>记录两件常做的事:让一台 Linux 能用密码或密钥 SSH 进另一台、在没有 root 的 Ubuntu 上启用 root 登录和免密。两者都绕不开 <code>/etc/ssh/sshd_config</code>。</p>\\n<h2>检查远端 SSH 配置</h2>\\n<p>连不上时,先别急着折腾本地。很多服务器默认就不允许密码认证,或者被安全软件/策略禁掉了密钥连接。先看远端 <code>sshd_config</code>:</p>\\n<div class=\\"language-bash line-numbers-mode\\" data-highlighter=\\"prismjs\\" data-ext=\\"sh\\"><pre><code><span class=\\"line\\"><span class=\\"token function\\">sudo</span> <span class=\\"token function\\">grep</span> <span class=\\"token string\\">'PermitRootLogin'</span> /etc/ssh/sshd_config</span>\\n<span class=\\"line\\"><span class=\\"token function\\">sudo</span> <span class=\\"token function\\">grep</span> <span class=\\"token string\\">'PasswordAuthentication'</span> /etc/ssh/sshd_config</span>\\n<span class=\\"line\\"><span class=\\"token function\\">sudo</span> <span class=\\"token function\\">vim</span> /etc/ssh/sshd_config</span>\\n<span class=\\"line\\"></span></code></pre>\\n<div class=\\"line-numbers\\" aria-hidden=\\"true\\" style=\\"counter-reset:line-number 0\\"><div class=\\"line-number\\"></div><div class=\\"line-number\\"></div><div class=\\"line-number\\"></div></div></div>"}`);export{d as comp,r as data};

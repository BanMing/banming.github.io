import{_ as a}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as t,a as n,o as s}from"./app-BR-OG0b9.js";const l={};function i(o,e){return s(),t("div",null,e[0]||(e[0]=[n(`<h1 id="eventsystem" tabindex="-1"><a class="header-anchor" href="#eventsystem"><span>EventSystem</span></a></h1><h2 id="c-delegates" tabindex="-1"><a class="header-anchor" href="#c-delegates"><span>C++ DELEGATES</span></a></h2><p>This system allows you to call member functions on C++ objects in a generic, yet type-safe way. Using delegates, you can dynamically bind to a member function of an arbitrary object, then call functions on the object, even if the caller doesn<code>t know the object</code>s type.</p><p>The system predefines various combinations of generic function signatures with which you can declare a delegate type from, filling in the type names for return value and parameters with whichever types you need.</p><p>Both single-cast and multi-cast delegates are supported, as well as &quot;dynamic&quot; delegates which can be serialized to disk and accessed from blueprints. Additionally, delegates may define &quot;payload&quot; data which will be stored and passed directly to bound functions.</p><h2 id="delegate-features" tabindex="-1"><a class="header-anchor" href="#delegate-features"><span>DELEGATE FEATURES</span></a></h2><p>Currently we support delegate signatures using any combination of the following:</p><ul><li>Functions returning a value <ul><li>Up to four &quot;payload&quot; variables</li></ul></li><li>Multiple function parameters depending on macro/template declaration</li><li>Functions declared as <code>const</code></li></ul><p>Multi-cast delegates are also supported, using the <code>DECLARE_MULTICAST_DELEGATE...</code> macros.</p><p>Multi-cast delegates allow you to attach multiple function delegates, then execute them all at once by calling a single &quot;Broadcast()&quot; function.</p><p>Multi-cast delegate signatures are not allowed to use a return value.</p><p>Unlike other types, dynamic delegates are integrated into the UObject reflection system and can be bound to blueprint-implemented functions or serialized to disk. You can also bind native functions, but the native functions need to be declared with <code>UFUNCTION</code> markup. You do not need to use <code>UFUNCTION</code> for functions bound to other types of delegates.</p><p>You can assign &quot;payload data&quot; to your delegates! These are arbitrary variables that will be passed directly to any bound function when it is invoked. This is really useful as it allows you to store parameters within the delegate it self at bind-time. All delegate types (except for &quot;dynamic&quot;) supports payload variables automatically!</p><p>When binding to a delegate, you can pass payload data along. This example passes two custom variables, a bool and an int32 to a delegate. Then when the delegate is invoked, these parameters will be passed to your bound function. The extra variable arguments must always be accepted after the delegate type parameter arguments.</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line">MyDelegate<span class="token punctuation">.</span><span class="token function">BindStatic</span><span class="token punctuation">(</span> <span class="token operator">&amp;</span>MyFunction<span class="token punctuation">,</span> <span class="token boolean">true</span><span class="token punctuation">,</span> <span class="token number">20</span> <span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>Remember to look at the signature table at the bottom of this documentation comment for the macro names to use for each function signature type, and the binding table to see options and concerns for binding.</p><h2 id="delegates-example" tabindex="-1"><a class="header-anchor" href="#delegates-example"><span>DELEGATES EXAMPLE</span></a></h2><p>Suppose you have a class with a method that you\`d like to be able to call from anywhere:</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line"><span class="token keyword">class</span> <span class="token class-name">FLogWriter</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">	<span class="token keyword">void</span> <span class="token function">WriteToLog</span><span class="token punctuation">(</span> FString <span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>To call the WriteToLog function, we<code>ll need to create a delegate type for that function</code>s signature. To do this, you will first declare the delegate using one of the macros below. For example, here is a simple delegate type:</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line"><span class="token function">DECLARE_DELEGATE_OneParam</span><span class="token punctuation">(</span> FStringDelegate<span class="token punctuation">,</span> FString <span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>This creates a delegate type called <code>FStringDelegate</code> that takes a single parameter of type <code>FString</code>.</p><p>Here<code>s an example of how you</code>d use this <code>FStringDelegate</code> in a class:</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line"><span class="token keyword">class</span> <span class="token class-name">FMyClass</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">	FStringDelegate WriteToLogDelegate<span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>This allows your class to hold a pointer to a method in an arbitrary class. The only thing the class really knows about this delegate is it\`s function signature.</p><p>Now, to assign the delegate, simply create an instance of your delegate class, passing along the class that owns the method as a template parameter. You<code>ll also pass the instance of your object and the actual function address of the method. So, here we</code>ll create an instance of our <code>FLogWriter</code> class, then create a delegate for the <code>WriteToLog</code> method of that object instance:</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line">FSharedRef<span class="token operator">&lt;</span> FLogWriter <span class="token operator">&gt;</span> <span class="token function">LogWriter</span><span class="token punctuation">(</span> <span class="token keyword">new</span> <span class="token function">FLogWriter</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line">WriteToLogDelegate<span class="token punctuation">.</span><span class="token function">BindSP</span><span class="token punctuation">(</span> LogWriter<span class="token punctuation">,</span> <span class="token operator">&amp;</span>FLogWriter<span class="token double-colon punctuation">::</span>WriteToLog <span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>You\`ve just dynamically bound a delegate to a method of a class! Pretty simple, right?</p><p>Note that the <code>SP</code> part of <code>BindSP</code> stands for <code>shared pointer</code>, because we<code>re binding to an object that</code>s owned by a shared pointer. There are versions for different object types, such as BindRaw() and BindUObject(). You can bind to global function pointers with BindStatic().</p><p>Now, your <code>WriteToLog</code> method can be called by FMyClass without it even knowing anything about the <code>FLogWriter</code> class! To call a your delegate, just use the <code>Execute()</code> method:</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line">WriteToLogDelegate<span class="token punctuation">.</span><span class="token function">Execute</span><span class="token punctuation">(</span> <span class="token function">TEXT</span><span class="token punctuation">(</span> <span class="token string">&quot;Delegates are spiffy!&quot;</span> <span class="token punctuation">)</span> <span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>If you call Execute() before binding a function to the delegate, an assertion will be triggered. In many cases, you\`ll instead want to do this:</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line">WriteToLogDelegate<span class="token punctuation">.</span><span class="token function">ExecuteIfBound</span><span class="token punctuation">(</span> <span class="token function">TEXT</span><span class="token punctuation">(</span> <span class="token string">&quot;Only executes if a function was bound!&quot;</span> <span class="token punctuation">)</span> <span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>That\`s pretty much all there is to it!! You can read below for a bit more information.</p><h2 id="more-information" tabindex="-1"><a class="header-anchor" href="#more-information"><span>MORE INFORMATION</span></a></h2><p>The delegate system understands certain types of objects, and additional features are enabled when using these objects. If you bind a delegate to a member of a UObject or shared pointer class, the delegate system can keep a weak reference to the object, so that if the object gets destroyed out from underneath the delegate, you\`ll be able to handle these cases by calling IsBound() or ExecuteIfBound() functions. Note the special binding syntax for the various types of supported objects.</p><p>It\`s perfectly safe to copy delegate objects. Delegates can be passed around by value but this is generally not recommended since they do have to allocate memory on the heap. Pass them by reference when possible!</p><p>Delegate signature declarations can exist at global scope, within a namespace or even within a class declaration (but not function bodies.)</p><h2 id="function-signatures" tabindex="-1"><a class="header-anchor" href="#function-signatures"><span>FUNCTION SIGNATURES</span></a></h2><p>Use this table to find the declaration macro to use to declare your delegate. The full list is defined in DelegateCombinations.h</p><table><thead><tr><th>Function signature</th><th>Declaration macro</th></tr></thead><tbody><tr><td>void Function()</td><td>DECLARE_DELEGATE( DelegateName )</td></tr><tr><td>void Function( &lt;Param1&gt; )</td><td>DECLARE_DELEGATE_OneParam( DelegateName, Param1Type )</td></tr><tr><td>void Function( &lt;Param1&gt;, &lt;Param2&gt; )</td><td>DECLARE_DELEGATE_TwoParams( DelegateName, Param1Type, Param2Type )</td></tr><tr><td>void Function( &lt;Param1&gt;, &lt;Param2&gt;, ... )</td><td>DECLARE_DELEGATE_&lt;Num&gt;Params( DelegateName, Param1Type, Param2Type, ... )</td></tr><tr><td>&lt;RetVal&gt; Function()</td><td>DECLARE_DELEGATE_RetVal( RetValType, DelegateName )</td></tr><tr><td>&lt;RetVal&gt; Function( &lt;Param1&gt; )</td><td>DECLARE_DELEGATE_RetVal_OneParam( RetValType, DelegateName, Param1Type )</td></tr><tr><td>&lt;RetVal&gt; Function( &lt;Param1&gt;, &lt;Param2&gt; )</td><td>DECLARE_DELEGATE_RetVal_TwoParams( RetValType, DelegateName, Param1Type, Param2Type )</td></tr><tr><td>&lt;RetVal&gt; Function( &lt;Param1&gt;, &lt;Param2&gt;, ... )</td><td>DECLARE_DELEGATE_RetVal_&lt;Num&gt;Params( RetValType, DelegateName, Param1Type, Param2Type, ... )</td></tr></tbody></table><p>Remember, there are three different delegate types you can define (any of the above signatures will work):</p><ul><li>Single-cast delegates: DECLARE_DELEGATE...()</li><li>Multi-cast delegates: DECLARE_MULTICAST_DELEGATE...()</li><li>Dynamic (UObject, serializable) delegates: DECLARE_DYNAMIC_DELEGATE...()</li></ul><h2 id="binding-and-safety" tabindex="-1"><a class="header-anchor" href="#binding-and-safety"><span>BINDING AND SAFETY</span></a></h2><p>Once a delegate has been declared, it can be bound to functions stored in different places.</p><p>Because delegates are often called long after they are bound, extra attention must be paid to avoid crashes. This list is for single-cast, for multi-cast delegates, replace Bind in the table below with Add. Also for multi-cast delegates, Add will return a handle that can then be used to later remove the binding. All multi-cast delegates have an ::FDelegate subtype defining an equivalent single-cast version, that can be Created one place and then added later.</p><table><thead><tr><th>Bind function</th><th>Usage</th></tr></thead><tbody><tr><td>BindStatic(&amp;GlobalFunctionName)</td><td>Call a static function, can either be globally scoped or a class static</td></tr><tr><td>BindUObject(UObject, &amp;UClass::Function)</td><td>Call a UObject class member function via a TWeakObjectPtr, will not be called if object is invalid</td></tr><tr><td>BindSP(SharedPtr, &amp;FClass::Function)</td><td>Call a native class member function via a TWeakPtr, will not be called if shared pointer is invalid</td></tr><tr><td>BindThreadSafeSP(SharedPtr, &amp;FClass::Function)</td><td>Call a native class member function via a TWeakPtr, will not be called if shared pointer is invalid</td></tr><tr><td>BindRaw(RawPtr, &amp;FClass::Function)</td><td>Call a native class member function with no safety checks. You MUST call Unbind or Remove when object dies to avoid crashes!</td></tr><tr><td>BindLambda(Lambda)</td><td>Call a lambda function with no safety checks. You MUST make sure all captures will be safe at a later point to avoid crashes!</td></tr><tr><td>BindSPLambda(SharedPtr, Lambda)</td><td>Call a lambda function only if shared pointer is still valid. Captured <code>this</code> will always be valid but any other captures may not be</td></tr><tr><td>BindWeakLambda(UObject, Lambda)</td><td>Call a lambda function only if UObject is still valid. Captured <code>this</code> will always be valid but any other captures may not be</td></tr><tr><td>BindUFunction(UObject, FName(&quot;FunctionName&quot;))</td><td>Usable for both native and dynamic delegates, will call a UFUNCTION with specified name</td></tr><tr><td>BindDynamic(UObject, &amp;UClass::FunctionName)</td><td>Convenience wrapper only available for dynamic delegates, FunctionName must be declared as a UFUNCTION</td></tr></tbody></table><h2 id="reference" tabindex="-1"><a class="header-anchor" href="#reference"><span>Reference</span></a></h2><ul><li><code>C:\\Program Files\\Epic Games\\UE_5.4\\Engine\\Source\\Runtime\\Core\\Public\\Delegates\\Delegate.h</code></li></ul>`,49)]))}const r=a(l,[["render",i],["__file","EventSystem.html.vue"]]),p=JSON.parse('{"path":"/GameEngine/Unreal/manual/EventSystem.html","title":"EventSystem","lang":"en-US","frontmatter":{"date":"2024-08-05T00:10:01.000Z","tag":["Unreal Engine"]},"headers":[{"level":2,"title":"C++ DELEGATES","slug":"c-delegates","link":"#c-delegates","children":[]},{"level":2,"title":"DELEGATE FEATURES","slug":"delegate-features","link":"#delegate-features","children":[]},{"level":2,"title":"DELEGATES EXAMPLE","slug":"delegates-example","link":"#delegates-example","children":[]},{"level":2,"title":"MORE INFORMATION","slug":"more-information","link":"#more-information","children":[]},{"level":2,"title":"FUNCTION SIGNATURES","slug":"function-signatures","link":"#function-signatures","children":[]},{"level":2,"title":"BINDING AND SAFETY","slug":"binding-and-safety","link":"#binding-and-safety","children":[]},{"level":2,"title":"Reference","slug":"reference","link":"#reference","children":[]}],"git":{"createdTime":1722874234000,"updatedTime":1722874234000,"contributors":[{"name":"BanMing","email":"ban-ming@foxmail.com","commits":1}]},"readingTime":{"minutes":4.6,"words":1380},"filePathRelative":"GameEngine/Unreal/manual/EventSystem.md","localizedDate":"August 5, 2024","excerpt":"\\n<h2>C++ DELEGATES</h2>\\n<p>This system allows you to call member functions on C++ objects in a generic, yet type-safe way.\\nUsing delegates, you can dynamically bind to a member function of an arbitrary object, then call functions on the object, even if the caller doesn<code>t know the object</code>s type.</p>"}');export{r as comp,p as data};

import{_ as a}from"./plugin-vue_export-helper-DlAUqK2U.js";import{e as t,f as r,o as s}from"./app-_JojGnWZ.js";const n={};function i(o,e){return s(),t("div",null,e[0]||(e[0]=[r(`<h1 id="p4-stream-与-sparse-stream" tabindex="-1"><a class="header-anchor" href="#p4-stream-与-sparse-stream"><span>P4 Stream 与 Sparse Stream</span></a></h1><p>Stream 是 P4 的分支模型,比手工维护 branch mapping 省心很多——分支视图自动生成 workspace view 和 branch view。这篇记 Stream 的基本概念、四种路径类型、创建流程,以及更轻量的 Sparse Stream 和并行 sync 配置。</p><h2 id="stream-基础" tabindex="-1"><a class="header-anchor" href="#stream-基础"><span>Stream 基础</span></a></h2><p>Stream 存放在 Stream depot 里,这是一种与标准 depot 平行的特殊 depot。</p><p>三种标准 Stream 类型:</p><ul><li><strong>Mainline</strong>:整个 stream 体系的主干。</li><li><strong>Development</strong>:长期项目、大的新功能。</li><li><strong>Release</strong>:修 bug、测试、发布分发。</li></ul><p>另外两种:</p><ul><li><strong>Virtual stream</strong>:用来收窄匹配范围,只在 stream view 里 share 需要的目录同步到本地。它不是真正的分支,提交会直接传到上游父分支,不能在此分支做合并。</li><li><strong>Task stream</strong>:轻量级分支,做 bug fix 和新功能。它的大部分元数据是临时存在一组私有表里,task stream 被删除或卸载时,它在未改动文件上的元数据也一并移除,对 P4 服务影响很小。它只对工作在这个分支的 workspace 可见,别人看不到。如果在这个分支上的人数到一定规模,可以转成 Development Stream。</li></ul><p>流转方向是 <strong>Merge down(从父往子合)&amp; Copy up(从子往父推)</strong>。</p><h2 id="stream-路径类型" tabindex="-1"><a class="header-anchor" href="#stream-路径类型"><span>Stream 路径类型</span></a></h2><p>Stream view 用一组路径加路径类型来描述这个 stream 包含哪些文件、能做哪些操作:</p><ul><li><strong>Share</strong>:可 sync、submit、integrate(branch/merge/copy)。改动会在 stream 间流动,主线文件一般都是 share。</li><li><strong>Isolate</strong>:可在本 stream 内 edit、submit,但改动不能 integrate 到别的 stream。适合新功能开发——在本地管理它的历史,稳定了再挪到 share 给别人 sync,或者整个丢弃。在那之前不会干扰别人,也不影响主线稳定。</li><li><strong>Import</strong>:可 sync,但不能 edit 或 integrate。适合第三方库这类构建需要、但从不改动的成分。</li><li><strong>Exclude</strong>:不能 sync、submit、integrate。</li></ul><table><thead><tr><th>路径类型</th><th>Merge/Copy</th><th>Branch</th><th>Sync</th><th>Submit</th></tr></thead><tbody><tr><td>share</td><td>Y</td><td>Y</td><td>Y</td><td>Y</td></tr><tr><td>isolate</td><td>N</td><td>N</td><td>Y</td><td>Y</td></tr><tr><td>import</td><td>N</td><td>N</td><td>Y</td><td>N</td></tr><tr><td>exclude</td><td>N</td><td>N</td><td>N</td><td>N</td></tr></tbody></table><h3 id="路径语法" tabindex="-1"><a class="header-anchor" href="#路径语法"><span>路径语法</span></a></h3><p>Stream path 语法跟 depot path 类似,去掉前导的 <code>//depot名</code> 和 stream 名:</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code><span class="line">Depot path:  //Stream/main/code/...</span>
<span class="line">Stream path: code/...</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>一条 stream view 由三部分组成:</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code><span class="line">Path_Type  Stream_Path  [Reference_Stream_Path]</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>Reference_Stream_Path</code> 用于从别的 depot 导入文件,例如 <code>import data/... //Test/data/...</code> 表示把 depot <code>Test</code> 里的 <code>data</code> 文件夹导入到本 stream 根下。</p><p>注意:stream view 不允许通配符 <code>*</code>、位置符 <code>%%1</code>、overlay 映射 <code>+</code>。</p><h3 id="父子继承" tabindex="-1"><a class="header-anchor" href="#父子继承"><span>父子继承</span></a></h3><p>子 stream 从父 stream 继承文件夹路径和行为规则:</p><ul><li><strong>Permissiveness(允许哪些操作)</strong>:子 stream 永远不能比父更宽松。四种路径类型从宽到严是 share &gt; isolate &gt; import &gt; exclude。</li><li><strong>Inclusiveness(包含哪些路径)</strong>:子不能比父更全。父没包含的文件夹路径,子里也不能加。</li></ul><h2 id="创建-stream" tabindex="-1"><a class="header-anchor" href="#创建-stream"><span>创建 Stream</span></a></h2><h3 id="创建-main-stream" tabindex="-1"><a class="header-anchor" href="#创建-main-stream"><span>创建 Main Stream</span></a></h3><p>所有 stream 都要在 Stream Depot 里,depot 由 IT 创建,数据管理者建主 stream。</p><ol><li><code>File &gt; New &gt; Stream...</code>,或在 Stream Graph 空白处右键 <code>New Stream...</code>。</li><li>填 stream 名(&quot;main&quot; 或 &quot;mainline&quot; 比较直观)。</li><li>Stream type 设为 <strong>mainline</strong>。</li><li>选这个 stream 落在哪个 stream depot。</li><li>(可选)勾上用这个 stream 立即建一个 workspace。</li><li>(可选)勾上创建后让你往里加文件。</li><li>勾 <strong>Locked</strong>,只让 stream owner 改主 stream 的设置。</li><li>取消勾 <strong>submitting restricted to stream owner</strong>,否则别人没法把自己 stream 的改动 copy up 到 main。</li><li>编辑 Paths(通常就一句 <code>share ...</code>,表示 main 里所有文件都可同步、提交、集成)。</li><li>点 OK。如果第 6 步勾了 populate,会弹窗问怎么往新 main stream 加文件:从本地文件系统 copy,或从 server depot branch。前者用 Add Files Wizard 拖拽添加;后者填 source files/folders 后 Branch。</li></ol><h3 id="创建-development-stream" tabindex="-1"><a class="header-anchor" href="#创建-development-stream"><span>创建 Development Stream</span></a></h3><p>用 Stream 功能必须工作在 Stream workspace 里。Stream workspace 跟标准 workspace 不同,它的映射从 stream view 自动生成、不可手编。</p><ol><li>Stream Graph 里右键 <strong>main</strong>,选 <code>Create New Stream from &#39;main&#39;...</code>。</li><li>填 stream 名。</li><li>Stream type 选 <strong>development</strong>。</li><li>Change propagation 勾 <strong>To parent</strong> 和 <strong>From parent</strong>。</li><li>确认父是 <strong>main</strong>。</li><li>勾 <strong>Locked</strong> 和 <strong>Submitting Restricted to stream owner</strong>,这样别人能 sync 但不能 submit。</li><li>编辑 Paths。建议只把你会改的文件放 share 路径(copy up 前 share 路径里的文件都得是最新的),构建需要但不改的放 import 路径。</li><li>点 OK,Stream Graph 里出现新 stream。</li><li>如果之前没勾 branch from parent,会出现绿色 merge down 箭头,双击 stream 选 <strong>Merge</strong>。</li><li>合并后 share 路径文件被同步,import 路径文件要手动 sync。</li></ol><h3 id="提交到自己的-dev-stream-并-copy-up-到-main" tabindex="-1"><a class="header-anchor" href="#提交到自己的-dev-stream-并-copy-up-到-main"><span>提交到自己的 dev stream 并 copy up 到 main</span></a></h3><ol><li>往自己 dev stream 提交,步骤和标准 depot 一样。</li><li>提交后 Stream Graph 出现绿色 copy up 箭头,双击 stream 选 <strong>Copy</strong>。</li><li>P4 会要求切到工作在 &quot;main&quot; 的 workspace,没有就新建一个。</li><li>Copy 对话框里改 changelist 描述。可勾 <strong>Do not copy newly branched target files to workspace</strong> 省磁盘。</li><li>Copy 完别的 dev stream 上会出现绿色 merge down 箭头,别人就能拿到你的改动。</li><li>如果 copy up 箭头是橙色、merge down 箭头是绿色,说明 main 变了、你的 dev stream 过时了,copy up 之前要先从 main merge 下来。</li></ol><p>切换 stream 时,如果两个 stream 的 workspace root 不同,本机会有两份文件副本。虽然占空间,但分开 root 更安全,能避免文件冲突。多数情况下开发者只需要保证自己 stream 的 workspace 最新,因为 copy up 前本来就强制要更新文件。</p><h2 id="sparse-stream" tabindex="-1"><a class="header-anchor" href="#sparse-stream"><span>Sparse Stream</span></a></h2><p>Sparse stream 是更轻量的分支方式:文件只在需要时才 branch,新 stream 不会从父 stream 那里搬来全部元数据,因此<strong>每个分支存的数据量大幅减少</strong>,省时省资源。适合 &quot;branch per bug&quot; / &quot;branch per feature&quot; 这种用法。</p><p>好处:</p><ul><li>瞬间新建分支。</li><li>每分支存储量显著下降。</li><li>省时省资源。</li></ul><p>限制:</p><ul><li>不支持部分集成(只允许 <code>@&lt;=change</code> 这种 revision 限制)。</li><li>不能 reparent。</li><li>不能从别的 stream unshelve。</li><li>不支持 undo。</li><li>不能有子 stream,例外是可以有一层 virtual stream(且这个 virtual 子不能再有子)。</li><li>非 sparse stream 不能转成 sparse stream。</li><li>不能作为其他 stream 的 component 被消费。</li></ul><h3 id="创建-sparse-stream" tabindex="-1"><a class="header-anchor" href="#创建-sparse-stream"><span>创建 sparse stream</span></a></h3><p>新建 stream 时类型选 <strong>development (sparse)</strong> 或 <strong>release (sparse)</strong>。在 advanced 设置里,Paths 参数可以改成需要的父分支 CL 节点,点 OK。</p><p>由于 sparse stream 不会自动 branch 父分支文件,创建后 depot tree 里看不到它,P4 server 也不产生存储数据。</p><h3 id="使用-sparse-stream" tabindex="-1"><a class="header-anchor" href="#使用-sparse-stream"><span>使用 sparse stream</span></a></h3><p>在 depot tree 里 checkout 父分支的文件,文件就会 branch 到 sparse stream 供使用。每 branch 一个文件到 sparse stream 都会生成一条提交记录。</p><h3 id="更新父分支-cl-到-sparse-stream" tabindex="-1"><a class="header-anchor" href="#更新父分支-cl-到-sparse-stream"><span>更新父分支 CL 到 sparse stream</span></a></h3><ul><li><strong>方法一</strong>:把父分支 merge 到 sparse 分支,会把 sparse stream 对应的父分支 CL 号更新到最新。</li><li><strong>方法二</strong>:在 sparse stream spec 里直接改 share CL,或用命令 <code>p4 merge @CL</code> 改到指定 CL 号。此时 Pending 面板会出现对 stream spec 的修改和需要解决冲突的文件,提交这个 spec 修改会应用到所有用该 sparse stream 的 workspace。</li></ul><h3 id="转成普通-stream" tabindex="-1"><a class="header-anchor" href="#转成普通-stream"><span>转成普通 stream</span></a></h3><p>右键要转的 sparse stream,选 &quot;Check out and convert sparse stream&quot;,点 convert。这会把父分支所有文件 branch 到当前分支,提交 pending 后完成转换。<strong>转换不可逆。</strong></p><p>参考:<a href="https://www.perforce.com/manuals/p4guide/Content/P4Guide/streams-sparse.html" target="_blank" rel="noopener noreferrer">Sparse streams</a>、<a href="https://www.perforce.com/webinars/vcs/introducing-sparse-streams" target="_blank" rel="noopener noreferrer">Introducing sparse streams 网络研讨会</a>。</p><h2 id="开启并行-sync" tabindex="-1"><a class="header-anchor" href="#开启并行-sync"><span>开启并行 Sync</span></a></h2><p>命令行直接指定并行度,<code>--parallel=0</code> 表示用服务器配置的最大并行:</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code><span class="line">p4 sync --parallel=0 //test/...</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><div class="hint-container warning"><p class="hint-container-title">Warning</p><p>P4V 默认就是 Parallel Sync(用服务器设置的最大并行数)。Log 里只会显示一行 <code>p4 sync e:\\Pilot\\...#head</code>,但在 P4D 的 monitor 里能看到多个 transmit 命令同时跑。</p></div><p>P4V、UGS 里也有各自的并行 sync 选项,在偏好/同步设置里开。</p>`,54)]))}const m=a(n,[["render",i],["__file","p4-stream.html.vue"]]),d=JSON.parse('{"path":"/Tools/VCS/P4/p4-stream.html","title":"P4 Stream 与 Sparse Stream","lang":"en-US","frontmatter":{"date":"2024-11-24T02:59:02.000Z","tag":["Tools","VCS","Perforce"]},"git":{"createdTime":1782480255000,"updatedTime":1782480255000,"contributors":[{"name":"BanMing","username":"BanMing","email":"ban-ming@foxmail.com","commits":1,"url":"https://github.com/BanMing"},{"name":"Claude Opus 4.7","username":"Claude Opus 4.7","email":"noreply@anthropic.com","commits":1,"url":"https://github.com/Claude Opus 4.7"}]},"readingTime":{"minutes":6.42,"words":1925},"filePathRelative":"Tools/VCS/P4/p4-stream.md","localizedDate":"November 24, 2024","excerpt":"\\n<p>Stream 是 P4 的分支模型,比手工维护 branch mapping 省心很多——分支视图自动生成 workspace view 和 branch view。这篇记 Stream 的基本概念、四种路径类型、创建流程,以及更轻量的 Sparse Stream 和并行 sync 配置。</p>\\n<h2>Stream 基础</h2>\\n<p>Stream 存放在 Stream depot 里,这是一种与标准 depot 平行的特殊 depot。</p>\\n<p>三种标准 Stream 类型:</p>\\n<ul>\\n<li><strong>Mainline</strong>:整个 stream 体系的主干。</li>\\n<li><strong>Development</strong>:长期项目、大的新功能。</li>\\n<li><strong>Release</strong>:修 bug、测试、发布分发。</li>\\n</ul>"}');export{m as comp,d as data};

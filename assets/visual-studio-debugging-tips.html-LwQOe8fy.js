import{_ as n}from"./plugin-vue_export-helper-DlAUqK2U.js";import{e as a,f as e,o as t}from"./app-_JojGnWZ.js";const i="/assets/leak-mem-usage-wCcIZwSP.png",o="/assets/leak-snapshot-Cted9BZq.png",p="/assets/leak-diff-B_0JIV0P.png",l={};function r(c,s){return t(),a("div",null,s[0]||(s[0]=[e('<h1 id="visual-studio-调试技巧合集" tabindex="-1"><a class="header-anchor" href="#visual-studio-调试技巧合集"><span>Visual Studio 调试技巧合集</span></a></h1><p>几个在大代码库里查 bug 时常用、但容易被忽略的 VS 调试手段：数据断点定位内存被谁改的、诊断工具抓内存泄漏、看 <code>std::mutex</code> 被哪个线程锁住，以及远程调试和临时关函数优化。</p><h2 id="数据断点-抓「这块内存是谁改的」" tabindex="-1"><a class="header-anchor" href="#数据断点-抓「这块内存是谁改的」"><span>数据断点：抓「这块内存是谁改的」</span></a></h2><p>数据断点（Data Breakpoint）监视一块内存，一旦它被写就触发中断。它特别适合两类问题：在庞大代码库里查某个值被意外改写的逻辑 bug，以及定位内存破坏——直接告诉你<strong>哪一行代码写花了这块内存</strong>。</p><p>前提是先确定要监视的内存地址，通常是某个变量。下面用一个真实案例走一遍流程：<strong>角色会被墙卡住</strong>。</p><ol><li><strong>复现问题。</strong> 先稳定复现卡住的现象。</li><li><strong>用调试工具对比正常 / 异常状态。</strong> 对比发现：碰撞球（collision sphere）的位置没有跟着角色走，但又没有真正发生碰撞（碰撞发生时本应高亮）。所以要盯的是碰撞球的位置。</li><li><strong>找到控制位置的变量。</strong> 定位到控制角色位置的变量（这里是个 <code>m_Trans</code>，一个 vector4），在它身上准备下断点。</li><li><strong>创建数据断点。</strong><ul><li>用这个变量的地址创建数据断点；</li><li><code>m_Trans</code> 是 vector4，监视宽度选 <strong>8 字节</strong>（按需要覆盖的字段大小定）；</li><li>把其他断点都删掉，只留这个数据断点，避免噪声。</li></ul></li><li><strong>分析触发数据变化的代码。</strong> 继续运行，每次这块内存被写都会断下来。逐个看调用栈： <ul><li>代码块 1：碰撞发生时会<strong>修正</strong>位置；</li><li>代码块 2：会<strong>修改</strong>位置。</li><li>问题在于：所有对位置的修改都应该发生在位置修正<strong>之前</strong>。现在顺序反了，修正先跑、修改后跑，于是修正被覆盖，角色卡住。</li></ul></li><li><strong>修复并测试。</strong> 把代码块 2 移到代码块 1 之前，问题消失。</li></ol><p>数据断点的价值就在第 5 步——不用通读代码猜逻辑，让 CPU 硬件断点直接把每个写这块内存的现场和调用栈摆到你面前。</p><blockquote><p>注意硬件数据断点数量有限（x86/x64 通常 4 个），且监视宽度受限（1/2/4/8 字节）；超出范围 VS 会退化成软件断点，慢很多。</p></blockquote><h2 id="诊断工具-定位内存泄漏" tabindex="-1"><a class="header-anchor" href="#诊断工具-定位内存泄漏"><span>诊断工具：定位内存泄漏</span></a></h2><p>VS 自带的 Diagnostic Tools 抓内存泄漏很顺手，能直接告诉你泄漏的内存是在哪分配的。流程：</p><ol><li><p>先通过 Process Memory 曲线确认现象——比如发现<strong>每次关卡重载后内存只增不减</strong>。</p><figure><img src="'+i+'" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure></li><li><p>在每次关卡重载的时间点各打一个 <strong>Snapshot</strong>。</p><figure><img src="'+o+'" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure></li><li><p>对比快照，能看到某类对象的分配计数持续增长。</p><figure><img src="'+p+`" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure></li><li><p>双击调用栈直接跳到分配代码。</p></li><li><p>在那分析这块内存为什么没被释放。</p></li></ol><p>关键是<strong>用快照差异</strong>而不是看绝对值：两个稳态时间点之间多出来的、没被回收的分配，才是泄漏的嫌疑。</p><h2 id="看-std-mutex-被哪个线程锁住" tabindex="-1"><a class="header-anchor" href="#看-std-mutex-被哪个线程锁住"><span>看 std::mutex 被哪个线程锁住</span></a></h2><p>排查死锁时常想知道某个 <code>std::mutex</code> 当前是被哪个线程持有的。VS 默认的可视化器不显示这个，可以放一个 <code>mutex.natvis</code> 到：</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code><span class="line">/Documents/Visual Studio 2017/Visualizers/mutex.natvis</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>（对应你的 VS 版本目录）。之后在 watch 窗口展开 <code>std::mutex</code> 就能看到持有它的线程信息。具体的 natvis 写法见 <a href="https://stackoverflow.com/questions/55918497/how-to-see-locking-thread-of-stdmutex-in-visual-studio" target="_blank" rel="noopener noreferrer">这个 StackOverflow 回答</a>。</p><h2 id="远程调试另一台机器上的程序" tabindex="-1"><a class="header-anchor" href="#远程调试另一台机器上的程序"><span>远程调试另一台机器上的程序</span></a></h2><p>要调另一台机器上的程序（比如一个独占全屏的游戏，本机调试器会被它顶掉），用 <strong>Remote Windows Debugger</strong>：在目标机上跑对应 VS 版本的 Remote Tools（从微软官网下 <code>Remote Tools for Visual Studio</code>），本机 <code>Debug → Attach to Process</code> 填目标机的远程连接地址即可。详见 <a href="https://learn.microsoft.com/en-us/visualstudio/debugger/remote-debugging" target="_blank" rel="noopener noreferrer">Remote Debugging</a>。</p><h2 id="临时关掉某个函数的优化" tabindex="-1"><a class="header-anchor" href="#临时关掉某个函数的优化"><span>临时关掉某个函数的优化</span></a></h2><p>Release 下断点跳来跳去、变量被优化掉看不了值时，可以只对某个函数关优化，而不用整个工程切 Debug：</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp"><pre><code><span class="line"><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">pragma</span> <span class="token expression"><span class="token function">optimize</span><span class="token punctuation">(</span></span><span class="token string">&quot;&quot;</span><span class="token expression"><span class="token punctuation">,</span> off<span class="token punctuation">)</span></span></span></span>
<span class="line"><span class="token keyword">void</span> <span class="token function">FunctionIWantToDebug</span><span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">{</span></span>
<span class="line">    <span class="token comment">// 这里的代码不会被优化，断点和变量都正常</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">pragma</span> <span class="token expression"><span class="token function">optimize</span><span class="token punctuation">(</span></span><span class="token string">&quot;&quot;</span><span class="token expression"><span class="token punctuation">,</span> on<span class="token punctuation">)</span></span></span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>调完记得把 pragma 删掉，别带进提交。</p>`,22)]))}const g=n(l,[["render",r],["__file","visual-studio-debugging-tips.html.vue"]]),m=JSON.parse('{"path":"/Basic/debug/visual-studio-debugging-tips.html","title":"Visual Studio 调试技巧合集","lang":"en-US","frontmatter":{"date":"2024-12-25T00:00:00.000Z","tag":["Debug","C++","Tools"]},"git":{"createdTime":1782460963000,"updatedTime":1782460963000,"contributors":[{"name":"BanMing","username":"BanMing","email":"ban-ming@foxmail.com","commits":1,"url":"https://github.com/BanMing"},{"name":"Claude Opus 4.7","username":"Claude Opus 4.7","email":"noreply@anthropic.com","commits":1,"url":"https://github.com/Claude Opus 4.7"}]},"readingTime":{"minutes":3.73,"words":1119},"filePathRelative":"Basic/debug/visual-studio-debugging-tips.md","localizedDate":"December 25, 2024","excerpt":"\\n<p>几个在大代码库里查 bug 时常用、但容易被忽略的 VS 调试手段：数据断点定位内存被谁改的、诊断工具抓内存泄漏、看 <code>std::mutex</code> 被哪个线程锁住，以及远程调试和临时关函数优化。</p>\\n<h2>数据断点：抓「这块内存是谁改的」</h2>\\n<p>数据断点（Data Breakpoint）监视一块内存，一旦它被写就触发中断。它特别适合两类问题：在庞大代码库里查某个值被意外改写的逻辑 bug，以及定位内存破坏——直接告诉你<strong>哪一行代码写花了这块内存</strong>。</p>\\n<p>前提是先确定要监视的内存地址，通常是某个变量。下面用一个真实案例走一遍流程：<strong>角色会被墙卡住</strong>。</p>"}');export{g as comp,m as data};

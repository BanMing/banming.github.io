import{_ as a}from"./plugin-vue_export-helper-DlAUqK2U.js";import{e as t,f as s,o}from"./app-BeQLnLll.js";const i={};function n(l,e){return o(),t("div",null,e[0]||(e[0]=[s(`<h1 id="gitlab-运维与-ci-实践" tabindex="-1"><a class="header-anchor" href="#gitlab-运维与-ci-实践"><span>GitLab 运维与 CI 实践</span></a></h1><p>围绕 GitLab 攒的几块运维笔记:Releases 发布、Geo 副站、UI 解冲突、CI 分支过滤、Jenkins 拉 Shared Library。</p><h2 id="gitlab-releases-发布流程" tabindex="-1"><a class="header-anchor" href="#gitlab-releases-发布流程"><span>GitLab Releases 发布流程</span></a></h2><p>GitLab 的 Release 里挂版本包链接时,要求是 https 协议。所以如果自己搭了个内网下载服务器(比如 Tomcat),得先给它配上 https,否则链接填不进去。整体三步:搭 Tomcat → 配自签名证书走 https → 在 GitLab 上建 Release。</p><h3 id="搭-tomcat-当下载服务器" tabindex="-1"><a class="header-anchor" href="#搭-tomcat-当下载服务器"><span>搭 Tomcat 当下载服务器</span></a></h3><ol><li>装 JDK,配好 <code>JAVA_HOME</code>、<code>Path</code>(加 <code>%JAVA_HOME%\\bin</code>)、<code>CLASSPATH</code> 环境变量。<code>java -version</code> 能输出版本就算装好。</li><li>下 Tomcat 免安装压缩包解压,配 <code>CATALINA_HOME</code> 并把 <code>%CATALINA_HOME%\\bin</code> 加进 <code>Path</code>。</li><li>双击 <code>bin\\startup.bat</code> 启动(窗口别关),<code>shutdown.bat</code> 停。浏览器开 <code>http://localhost:8080/</code> 能出页面就 OK。</li><li>把版本发布目录(例:<code>D:\\MyProject_Release</code>)设成共享文件夹并配好权限,再在 Tomcat 里建一个映射这个目录的配置,这样局域网里就能 <code>http://&lt;server-ip&gt;:8080/shareFolder/...</code> 访问到包了。</li></ol><h3 id="配-https" tabindex="-1"><a class="header-anchor" href="#配-https"><span>配 https</span></a></h3><p>第一步搭的 Tomcat 是 http,而 Release 链接必须 https,所以要给 Tomcat 配自签名证书走 https。具体证书生成和 <code>server.xml</code> 配置网上 Tomcat https 教程很多,这里不展开。</p><h3 id="在-gitlab-上建-release" tabindex="-1"><a class="header-anchor" href="#在-gitlab-上建-release"><span>在 GitLab 上建 Release</span></a></h3><p>GitLab 官方文档:<code>https://gitlab.example.com/help/user/project/releases/index</code></p><ol><li>进项目的 Releases 页面创建 Release。</li><li>没有 tag 的话可以现场建一个新 tag,并选好对应分支。</li><li>填说明。上传版本包有两种方式:notes 里直接拖入文件,或在 URL 处填版本包的 https 链接(就是上面 Tomcat 提供的那个)。</li><li>保存即发布。</li></ol><h2 id="gitlab-geo-副站" tabindex="-1"><a class="header-anchor" href="#gitlab-geo-副站"><span>GitLab Geo 副站</span></a></h2><p>Geo 是 GitLab 的异地副站方案,文档见 <code>https://docs.gitlab.com/ee/administration/geo/index.html</code>。副站支持 pull 和 push,<strong>但推大文件时必须切回主站(Primary Site)</strong>,这是用 Geo 时最容易踩的坑。</p><h3 id="申请副站" tabindex="-1"><a class="header-anchor" href="#申请副站"><span>申请副站</span></a></h3><p>走 IT 工单要一台服务器,参考配置:</p><ul><li>CPU 4C8T</li><li>内存:16GB 物理 + 12GB swap</li><li>磁盘:50G 系统盘 + 500G 数据盘(可扩)</li><li>系统:AlmaLinux 8.6</li></ul><p>拿到机器后再开一张工单给 IT,把这台机器和主站信息给他们,让他们把 Geo 副站搭起来。</p><h3 id="本地在主站-副站之间切换" tabindex="-1"><a class="header-anchor" href="#本地在主站-副站之间切换"><span>本地在主站/副站之间切换</span></a></h3><p>副站离自己近,日常拉代码快;但推大文件要切回主站。切换靠 <code>git config</code> 的 <code>url.insteadOf</code> 做地址改写。</p><p>全局切到副站(以下用占位符,换成自己环境的真实地址):</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line"><span class="token function">git</span> config <span class="token parameter variable">--global</span> url.<span class="token string">&quot;&lt;Geo 副站&gt;&quot;</span>.insteadOf <span class="token operator">&lt;</span>主站 SSH 地址<span class="token operator">&gt;</span></span>
<span class="line"><span class="token function">git</span> config <span class="token parameter variable">--global</span> url.<span class="token string">&quot;&lt;Geo 副站&gt;&quot;</span>.insteadOf <span class="token operator">&lt;</span>主站 https 地址<span class="token operator">&gt;</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 示例(占位地址):</span></span>
<span class="line"><span class="token function">git</span> config <span class="token parameter variable">--global</span> url.<span class="token string">&quot;git@gitlab-cd.example.com&quot;</span>.insteadOf git@gitlab.example.com</span>
<span class="line"><span class="token function">git</span> config <span class="token parameter variable">--global</span> url.<span class="token string">&quot;https://gitlab-cd.example.com/&quot;</span>.insteadOf https://gitlab.example.com/</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>全局切回主站(把上面加的改写规则删掉):</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code><span class="line"><span class="token function">git</span> config <span class="token parameter variable">--global</span> --remove-section url.<span class="token string">&quot;&lt;副站 SSH 地址&gt;&quot;</span></span>
<span class="line"><span class="token function">git</span> config <span class="token parameter variable">--global</span> --remove-section url.<span class="token string">&quot;&lt;副站 https 地址&gt;&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>如果只想对单个仓库切换,不动全局,直接改 <code>&lt;your_repo_path&gt;\\.git\\config</code> 里对应的 url 段即可。</p><p>提醒一句:push 大文件前,务必确认当前指向的是主站。</p><h2 id="在-gitlab-ui-里解合并冲突" tabindex="-1"><a class="header-anchor" href="#在-gitlab-ui-里解合并冲突"><span>在 GitLab UI 里解合并冲突</span></a></h2><p>两个分支改了同一文件的同一部分,Git 没法自动合并时就产生冲突,文档见 <code>https://docs.gitlab.com/ee/user/project/merge_requests/resolve_conflicts.html</code>。</p><p>UI 上能解冲突的前提条件(任一文件不满足就只能本地解):</p><ul><li>文件是文本,不是二进制</li><li>编码兼容 UTF-8</li><li>文件本身还没有冲突标记</li><li>加上冲突标记后文件不超过 200 KB</li><li>文件在两个分支里路径相同</li></ul><p>操作逻辑是给每段冲突选 ours 或 theirs,全部标记完就能 resolve。resolve 等价于把目标分支合进源分支——若源分支是 <code>feature</code>、目标是 <code>main</code>,相当于本地执行 <code>git checkout feature; git merge main</code>。</p><p>一个限制:<strong>在 UI 上解冲突,结果只能提交到源分支,不能提交到目标分支</strong>,这是为了防止把目标分支搞不稳定。如果非要把解冲突后的结果落到目标分支,得在本地切到目标分支合并、解冲突、测试通过后再提交。</p><p>另外 GitLab 检测不到&quot;重命名到不同路径&quot;这类冲突。比如分支 <code>a</code> 做 <code>git mv file1 file2</code>、分支 <code>b</code> 做 <code>git mv file1 file3</code>,合并后两个文件都会存在,不报冲突。</p><h2 id="ci-pipeline-按分支过滤-job" tabindex="-1"><a class="header-anchor" href="#ci-pipeline-按分支过滤-job"><span>CI Pipeline 按分支过滤 Job</span></a></h2><p>用 <code>only:refs</code> 和 <code>except:refs</code> 控制 job 在什么分支或什么触发类型下才加进 pipeline,文档见 <code>https://docs.gitlab.com/ee/ci/yaml/index.html</code>(only / except 一节)。它是 job 级关键字,只能写在 job 里。</p><p>输入可以是:</p><ul><li>分支名,如 <code>main</code>、<code>my-feature-branch</code></li><li>匹配分支名的正则,如 <code>/^feature-.*/</code></li><li>下面这些预定义关键字:</li></ul><table><thead><tr><th>值</th><th>含义</th></tr></thead><tbody><tr><td><code>api</code></td><td>由 pipelines API 触发的 pipeline</td></tr><tr><td><code>branches</code></td><td>Git 引用是分支时</td></tr><tr><td><code>chat</code></td><td>由 GitLab ChatOps 命令创建</td></tr><tr><td><code>external</code></td><td>使用 GitLab 之外的 CI 服务时</td></tr><tr><td><code>external_pull_requests</code></td><td>GitHub 上外部 PR 创建或更新时</td></tr><tr><td><code>merge_requests</code></td><td>MR 创建或更新时触发(启用 MR pipeline、merged results、merge trains)</td></tr><tr><td><code>pipelines</code></td><td>通过 API 或 <code>trigger</code> 关键字创建的多项目 pipeline</td></tr><tr><td><code>pushes</code></td><td><code>git push</code> 事件触发(含分支和 tag)</td></tr><tr><td><code>schedules</code></td><td>定时 pipeline</td></tr><tr><td><code>tags</code></td><td>Git 引用是 tag 时</td></tr><tr><td><code>triggers</code></td><td>用 trigger token 创建的 pipeline</td></tr><tr><td><code>web</code></td><td>在 GitLab UI 里点 Run pipeline 创建的</td></tr></tbody></table><blockquote><p><code>only</code> / <code>except</code> 现在算是老语法,新项目更推荐用 <code>rules</code>,但维护老 <code>.gitlab-ci.yml</code> 时还是会经常碰到。</p></blockquote><h2 id="jenkins-用-gitlab-拉-shared-library" tabindex="-1"><a class="header-anchor" href="#jenkins-用-gitlab-拉-shared-library"><span>Jenkins 用 GitLab 拉 Shared Library</span></a></h2><p>让 Jenkins 从 GitLab 拉 Pipeline Shared Library,走 SSH 密钥认证,流程是生成密钥 → 上传 → 测试。</p><h3 id="生成密钥" tabindex="-1"><a class="header-anchor" href="#生成密钥"><span>生成密钥</span></a></h3><p>在 Jenkins master 机器上用 Git Bash 执行 <code>ssh-keygen</code> 生成公私钥。</p><h3 id="上传密钥" tabindex="-1"><a class="header-anchor" href="#上传密钥"><span>上传密钥</span></a></h3><ol><li>把<strong>公钥</strong>上传到个人 GitLab 账号的 SSH Keys。</li><li>把<strong>私钥</strong>上传到 Jenkins 的 Credentials,注意类型要选 SSH(而不是 username/password)。</li></ol><h3 id="测试" tabindex="-1"><a class="header-anchor" href="#测试"><span>测试</span></a></h3><ol><li>在 Jenkins master 上 <code>git clone</code> 一下目标仓库,目的是生成 <code>known_hosts</code>(否则首次连接会卡在 host key 确认)。</li><li>如果用 TortoiseGit 拉,SSH client 要手动指向 <code>C:\\Program Files\\Git\\usr\\bin\\ssh.exe</code>,<strong>别用它默认的 Plink</strong>——Plink 和上面生成的 OpenSSH 密钥格式对不上。</li></ol>`,46)]))}const r=a(i,[["render",n],["__file","gitlab-ops.html.vue"]]),p=JSON.parse('{"path":"/Tools/VCS/Git/gitlab-ops.html","title":"GitLab 运维与 CI 实践","lang":"en-US","frontmatter":{"date":"2024-12-20T12:48:20.000Z","tag":["Tools","VCS","Git"]},"git":{"createdTime":1782480255000,"updatedTime":1782480255000,"contributors":[{"name":"BanMing","username":"BanMing","email":"ban-ming@foxmail.com","commits":1,"url":"https://github.com/BanMing"},{"name":"Claude Opus 4.7","username":"Claude Opus 4.7","email":"noreply@anthropic.com","commits":1,"url":"https://github.com/Claude Opus 4.7"}]},"readingTime":{"minutes":4.95,"words":1484},"filePathRelative":"Tools/VCS/Git/gitlab-ops.md","localizedDate":"December 20, 2024","excerpt":"\\n<p>围绕 GitLab 攒的几块运维笔记:Releases 发布、Geo 副站、UI 解冲突、CI 分支过滤、Jenkins 拉 Shared Library。</p>\\n<h2>GitLab Releases 发布流程</h2>\\n<p>GitLab 的 Release 里挂版本包链接时,要求是 https 协议。所以如果自己搭了个内网下载服务器(比如 Tomcat),得先给它配上 https,否则链接填不进去。整体三步:搭 Tomcat → 配自签名证书走 https → 在 GitLab 上建 Release。</p>\\n<h3>搭 Tomcat 当下载服务器</h3>\\n<ol>\\n<li>装 JDK,配好 <code>JAVA_HOME</code>、<code>Path</code>(加 <code>%JAVA_HOME%\\\\bin</code>)、<code>CLASSPATH</code> 环境变量。<code>java -version</code> 能输出版本就算装好。</li>\\n<li>下 Tomcat 免安装压缩包解压,配 <code>CATALINA_HOME</code> 并把 <code>%CATALINA_HOME%\\\\bin</code> 加进 <code>Path</code>。</li>\\n<li>双击 <code>bin\\\\startup.bat</code> 启动(窗口别关),<code>shutdown.bat</code> 停。浏览器开 <code>http://localhost:8080/</code> 能出页面就 OK。</li>\\n<li>把版本发布目录(例:<code>D:\\\\MyProject_Release</code>)设成共享文件夹并配好权限,再在 Tomcat 里建一个映射这个目录的配置,这样局域网里就能 <code>http://&lt;server-ip&gt;:8080/shareFolder/...</code> 访问到包了。</li>\\n</ol>"}');export{r as comp,p as data};
